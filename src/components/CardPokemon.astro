---
type Props = {
  id: number;
}

import {type pokemonResponse} from '../types/pokemonResponse'
import {getPokemonByUrl} from '../services/pokemons'
import TypesPokemon from './TypesPokemon.astro'

const { id } = Astro.props

---
<a
  href={`/pokemon/${id}`}
  class="rounded-lg border shadow-md bg-gray-800 border-gray-700 hover:scale-105 hover:bg-gray-700 hover:border-gray-500 transition flex flex-col"
>
<header class="p-4 flex-grow">
 
  <!-- {types.map(type => (
  <TypesPokemon name={type.type.name} url={type.type.url}/>
  ))} -->

  <h2> #{id}</h2>
</header>
  <!-- <picture >
    {sprites.front_shiny_female ? 
    <img class="mb-5 rounded-lg" src={sprites.front_shiny} alt={`looking for pokemon ${name} in the pokeball`} />
    <img class="mb-5 rounded-lg" src={sprites.front_shiny_female} alt={`looking for pokemon ${name} in the pokeball`} />
    :
    <img class="mb-5 rounded-lg" src={sprites.front_shiny} alt={`looking for pokemon ${name} in the pokeball`} />
    }
    
  </picture> -->
  <h2> #{id} Name:</h2>
</a>


